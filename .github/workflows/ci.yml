name: CI

on:
  push:
    branches: [ '**' ]  # ทุก branch
  pull_request:
    branches: [ main ]  # PR ที่จะ merge เข้า main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: npm ci
    - run: npm test